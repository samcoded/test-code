version: 2.1
jobs:
    print_name:
        docker:
          - image: circleci/node:13.8.0
        steps:
          - checkout
          - run: 
              name: "Print my name"
              command: echo $MY_NAME

workflows:
    myname:
        jobs:
          - print_name
            